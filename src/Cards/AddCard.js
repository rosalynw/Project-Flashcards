import React from "react";

function AddCard ({decks}) {
    return (
        <div>
            <nav>Home / / Add Card</nav>
            <h5>Name: Add Card</h5>
        </div>
    )
}

export default AddCard;